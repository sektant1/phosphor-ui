import { Meta } from "@storybook/addon-docs";

<Meta title="Zone/Introduction" />

# phosphor-ui — Zone Design System

Single-channel green phosphor. Retro / military / 80s terminal / Pip-Boy 3000 / STALKER GAMMA.

> // СЕКРЕТНО //  ЧЕРНОБЫЛЬСКАЯ ЗОНА ОТЧУЖДЕНИЯ

## Install

```bash
npm install @sektant1/phosphor-ui
```

```ts
import "@sektant1/phosphor-ui/tokens.css"; // design tokens (CSS vars + fonts)
import { Button, PdaWindow, PostListing } from "@sektant1/phosphor-ui";
```

## Palette

<div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: "8px" }}>
  {[
    ["--phosphor", "#2cff7a"],
    ["--phosphor-bright", "#b6ffce"],
    ["--phosphor-dim", "#1fb854"],
    ["--phosphor-fade", "#0a4d22"],
    ["--magenta", "#62ff9a"],
    ["--magenta-bright", "#d6ffe2"],
    ["--magenta-deep", "#157a3b"],
    ["--magenta-fade", "#06321a"],
    ["--bg", "#04140a"],
    ["--bg-raise", "#082416"],
    ["--bg-deep", "#010604"],
    ["--ink", "#d8ffe7"],
  ].map(([name, hex]) => (
    <div key={name} style={{ border: "1px solid var(--phosphor-fade)", padding: 8, fontFamily: "var(--font-code)", fontSize: 11 }}>
      <div style={{ background: hex, height: 40, marginBottom: 6 }} />
      <div style={{ color: "var(--phosphor)" }}>{name}</div>
      <div style={{ color: "var(--phosphor-fade)" }}>{hex}</div>
    </div>
  ))}
</div>

## Type specimens

<div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
  <div className="t-h1">// SECTOR-7 // АНОМАЛЬНАЯ АКТИВНОСТЬ</div>
  <div className="t-h2">transmission log</div>
  <div className="t-h3">▸ artifact recovery</div>
  <div className="t-h4">└─ side note</div>
  <div className="t-body">Pool reuse open DB connections. No new connection per request.</div>
  <div className="t-mono">grep -r "transmission" .</div>
  <div className="t-terminal">CH 0x4C · γ-2 · NORM</div>
  <div className="t-stamp">// LAST CONTACT //</div>
  <div className="t-prompt">~/zone-net $</div>
</div>

## Principles

- **No rounded corners.** `border-radius: 0` everywhere except LED dots.
- **No emoji.** Box-drawing glyphs only: ▌ ▸ ▾ █ ▓ ▒ ░ ☢ ◇.
- **Glow over shadows.** Emphasis = `text-shadow: var(--glow-emerald)`.
- **Inverted block hover.** `background: var(--phosphor); color: var(--bg)` — no transitions.
- **Mono fonts only.** `Space Mono`, `JetBrains Mono`, `VT323`.
