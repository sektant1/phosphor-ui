# phosphor notes

## TODOS: 
- make the checkbox strikethrough be toggleable
- make the cursor in the input only show when typing and make it blink animation
- delete the select component and change for the dropdown where it uses it
- make statpill more simple
- change the font and improve a bit the header-nav, also the mobile header nav shouldnt show to locale buttons(also need a mobile variant) and the tab variant(also the locale buttons) have a top/left only border on hover (shouldnt have this)
- change the design of the glyph in the callout, no border and bg around the glyph and it should be smaller(glyph)
- make the copy button in the codeblock smaller and same height of the language tag
- make the course card thumb use all the left square instead of a div inside that square. also make the progress bar and button be aligned horizontaly and the locked button should be on the same spot at the normal variant
- the form field is showing the error all the times
- postfrontmatter is too big, make it smaller
- share bar is too big, make it smaller
- delete the page template, and the home page should be the same placement of components as in the legacy demo, and the post page the same in the legacy post demo and all use the postbody, postheader. (use the legacy demo as a reference for positioning the components)



