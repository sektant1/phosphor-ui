# phosphor notes

## TODOS: 
<!-- 1. Fix/improve Storybook controls(controls breaking component, weird/wrong controls), docs, and example code(also the show code is showing different code from the live component preview) and make use of new storybook addons ive added -->
2. Add cursor animations to input(needs to follow the text and blink just like a 80's terminal) and put it in the components it fits
4. Add animation to the ascii banner component
3. Fix dim text with blur in some places
<!-- 4. Fix error checkbox (change green check effect to red cross effect) -->
<!-- 5. Redesign select component (entirely, needs to be completely different) -->
<!-- 6. Redesign status pill component -->
<!-- 7. Redesign switch component (entirely, needs to be completely different) -->
<!-- 8. Fix author card broken link CSS (currently using columns instead of rows) -->
<!-- 9. Redesign boot-nav component (entirely, needs to be completely different(also the mobile) you can make a Link variation to use here) -->
10. Redesign callouts for all sizes
11. Redesign codeblock frame
12. Make the course card have a thumb in the cover space like in the post row, remove the progress bar when locked and make the without art variant dont have the thumbnail and the cover space
<!-- 13. Redesign form field (currently totally broken) -->
14. Redesign content status badge
15. Create badge/chip component
16. Redesign lesson row
17. Redesign frontmatter (show only meaningful data for each content type)
18. Redesign prereq list to be only a list (remove checkboxes)
<!-- 19. Create `List` component (atom or molecule?) -->
20. Redesign seriesnav component and rethink whether it should exist
21. Redesign share bar component
22. Redesign stepper (merge with breadcrumb? merge footer with pagination?)
23. Fix timeline alignment
24. Fix login error state not showing red styling
25. Redesign content and course editors
    * Fix broken layouts
    * Replace incorrect column layouts with rows where needed
    * Make preview render actual MD/MDX page/post previews (including images)
    * Improve the raw typing UX in the editors(e.g. Tab right now is switching to another field instead of inserting a tab space)

26. Delete `FooterStencil` component
<!-- 27. Redesign header and create more variations (including mobile) -->
<!-- 28. Redesign locale switcher/component -->
29. Fix HeroFrame scanline animation stillness
30. Fix module accordion css/alignment
31. create more variations of nerdtree (including mobile)
32. Redesign the post body/header entirely
33. redesign the related posts (broken css layout)
34. redesign/improve the admin shell
<!-- 35. improve and add more variations to the button component -->
36. fix the layout components
37. check the necessity of the PageBase component
38. all the site pages, including post, have broken layout/css
39. redesign article list component
40. redesign the code block color pallete
41. improve README.md (quick setup,examples, recipes, installing (dont use the 'travessao' and also dont use the AI accent('this isnt just this, is also that')))
